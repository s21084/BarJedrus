version: 1.0
runtime: nodejs18
build:
  commands:    
    build:
      - npm install
      - npx expo install --fix              
run:                              
  command: npm start
  network:
    port: 8081